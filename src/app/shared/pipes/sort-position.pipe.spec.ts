import { SortPositionPipe } from './sort-position.pipe';

describe('SortPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
