import { DateSortDescPipe } from './date-sort-desc.pipe';

describe('DateSortDescPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSortDescPipe();
    expect(pipe).toBeTruthy();
  });
});
