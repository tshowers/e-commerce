import { TitleSortPipe } from './title-sort.pipe';

describe('TitleSortPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleSortPipe();
    expect(pipe).toBeTruthy();
  });
});
