import { ProductDependencyCalcPipe } from './product-dependency-calc.pipe';

describe('ProductDependencyCalcPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductDependencyCalcPipe();
    expect(pipe).toBeTruthy();
  });
});
