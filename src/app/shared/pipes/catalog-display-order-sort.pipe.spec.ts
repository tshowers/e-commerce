import { CatalogDisplayOrderSortPipe } from './catalog-display-order-sort.pipe';

describe('CatalogDisplayOrderSortPipe', () => {
  it('create an instance', () => {
    const pipe = new CatalogDisplayOrderSortPipe();
    expect(pipe).toBeTruthy();
  });
});
