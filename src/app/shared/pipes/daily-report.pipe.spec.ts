import { DailyReportPipe } from './daily-report.pipe';

describe('DailyReportPipe', () => {
  it('create an instance', () => {
    const pipe = new DailyReportPipe();
    expect(pipe).toBeTruthy();
  });
});
