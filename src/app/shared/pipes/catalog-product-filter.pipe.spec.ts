import { CatalogProductFilterPipe } from './catalog-product-filter.pipe';

describe('CatalogProductFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CatalogProductFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
