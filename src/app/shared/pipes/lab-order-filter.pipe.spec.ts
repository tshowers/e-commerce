import { LabOrderFilterPipe } from './lab-order-filter.pipe';

describe('LabOrderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LabOrderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
