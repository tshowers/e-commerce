import { AppointmentTimeSortPipe } from './appointment-time-sort.pipe';

describe('AppointmentTimeSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AppointmentTimeSortPipe();
    expect(pipe).toBeTruthy();
  });
});
